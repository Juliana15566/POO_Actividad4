public static void main(String[] args) {
        //Crea un programa que, utilizando un bucle while, muestre todos los números múltiplos de 8 en el rango de 0 a 100, 
        //permitiendo comprender la iteración y el uso del operador módulo para determinar divisibilidad.
        
        int interaccion=0;
        
        while(interaccion<=100){
            if(interaccion%8==0){
                System.out.println(interaccion);
            }
            interaccion++;
        }       
    }
